const PORT = 3000; // port to start server on
const MONGO_PORT = 27017; // mongo port
const DB_NAME = 'RMM'; // sample db name
const SECRET = "APISECRET";
const REDIS_PORT = 3030;

export {
	PORT,
	MONGO_PORT,
	DB_NAME,
	SECRET,
	REDIS_PORT
}